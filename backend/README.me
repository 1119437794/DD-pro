1.正式部署上线时，可以执行设置
    ENV(开发环境，默认是undefined)
    PORT(端口设置，默认是8080)

2.window下通过如：set ENV=production
  linux下通过如：export PORT=1234

3.对file-loader以及clean-webpack-plugin修改来适应项目需求

4. 运行npm test 编译ES6代码
   运行npm start 运行服务器【ES5代码】
